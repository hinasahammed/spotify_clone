import 'package:flutter/material.dart';

class SelectMusicType extends StatelessWidget {
  const SelectMusicType({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
